import { OuterLinkDirective } from './outer-link.directive';

describe('OuterLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new OuterLinkDirective();
    expect(directive).toBeTruthy();
  });
});
