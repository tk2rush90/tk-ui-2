import {SelectOverlay} from './select-overlay.component';

describe('SelectOptionsBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectOverlay();
    expect(directive).toBeTruthy();
  });
});
