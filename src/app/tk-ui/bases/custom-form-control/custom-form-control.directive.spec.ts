import {CustomFormControl} from './custom-form-control.directive';

describe('CustomFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomFormControl();
    expect(directive).toBeTruthy();
  });
});
